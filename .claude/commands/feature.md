---
description: Run the full agent team on a feature request (plan → build → review → fix until approved)
---

Implement the following feature request using the team workflow defined in CLAUDE.md:

$ARGUMENTS

Run the full loop: tech-lead plans, developer implements, qa-reviewer reviews
and runs `npm test`, bug-fixer fixes anything QA finds, and repeat until QA
returns APPROVED. Report the final result and which agents did what.
